{
	"definitions": {},
	"type": "object",
	"required": [
		"username",
		"password"
	],
	"properties": {
		"username": {
			"type": "string",
			"pattern": "^[a-z0-9_-]{3,50}$"
		},
		"password": {
			"type": "string",
			"pattern": "^(?=.*?[A-Z])(?=.*?[a-z])(?=.*?[0-9])(?=.*?[#?!@$ %^&*-]).{8,100}$"
		}
	}
}